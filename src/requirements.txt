# Core scientific stack
numpy==1.24.4
scipy==1.11.3
pandas==2.1.2
pyarrow==13.0.0
scikit-learn==1.5.2
statsmodels==0.14.0
joblib==1.3.2
numba==0.57.1
dask==2023.10.1

# Plotting
matplotlib==3.8.0
seaborn==0.13.0
wordcloud==1.9.3

# Survival analysis
lifelines==0.30.0
scikit-survival==0.23.1

# Clustering / dimensionality reduction / feature selection
hdbscan==0.8.33
umap-learn==0.5.5
pynndescent==0.5.11
hnswlib==0.8.0
stepmix==2.2.0
Boruta==0.3
kneed==0.8.5
xgboost==2.1.4

# NLP / text embeddings
gensim==4.3.2
sentence-transformers==2.3.1
transformers==4.37.2
torch==2.2.0
symspellpy==6.7.8
tensorflow==2.15.0.post1
tensorflow-hub==0.16.1
tensorflow-text==2.15.0

# Misc utilities
simplejson==3.20.1
debugpy==1.8.0

# Adjudication tool backend (optional; only needed if you build out the
# Flask/Mongo adjudication tool referenced in ops/, whose source is not
# part of this release)
flask==3.1.0
pymongo==4.11.1

# Optional: GPU-accelerated UMAP/HDBSCAN via RAPIDS cuML (used opportunistically
# by src/Top2Vec.py if importable). Not installable via pip; see
# https://docs.rapids.ai/install for conda/pip-nightly instructions matching
# your CUDA version.
# cuml
